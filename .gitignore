_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
.aider*
node_modules
# Raw recovery material stays out of git; the numbered .webp the flipbook
# serves are the live assets, so un-ignore those.
assets/img/paintings/*
!assets/img/paintings/*.webp
